version: 2

models:
  - name: stg_emissions_sources
    columns:
      - name: source_id
        data_tests:
          - not_null
      - name: country_code
        data_tests:
          - not_null
      - name: emissions_quantity
        data_tests:
          - not_null:
              config:
                severity: warn
